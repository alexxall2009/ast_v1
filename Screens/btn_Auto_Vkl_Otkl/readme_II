Главное, что нужно исправить в первую очередь

    onBack() вынести в LaunchedEffect — иначе команда отправляется десятки раз.
    mutableStateListOf в ViewModel — иначе UI не будет обновляться.
    updateBtn() сейчас не работает (== вместо = + val).
    isHasFocus возвращает не то поле.
    ModalBottomSheet вынести из цикла.
    Заменить строковые ключи на enum.
